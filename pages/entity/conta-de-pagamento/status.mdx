---
title: "Consultar situação da conta"
tag: "GET"
sidebarTitle: "Situação da conta"
---

Consulte a situação da conta de pagamento do lojista.

<Badge color="blue">GET</Badge>

```text
/api/payment-account/status
```

Use JWT. A resposta `200` retorna `status`, `can_receive_payments` e `message` em `data`.
