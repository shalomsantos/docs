---
title: "Detalhar cupom"
tag: "GET"
sidebarTitle: "Detalhar"
---

Consulte um cupom pelo seu UUID.

<Badge color="blue">GET</Badge>

```text
/api/coupons/{uuid}
```

Use JWT. A resposta `200` retorna o cupom em `data`. Se o UUID não existir ou não pertencer ao merchant, a API retorna `404`.
