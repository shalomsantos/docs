---
title: "Consultar notificações não lidas"
tag: "GET"
sidebarTitle: "Não lidas"
---

Consulte a quantidade de notificações não lidas.

<Badge color="blue">GET</Badge>

```text
/api/notifications/unread-count
```

Use JWT. A resposta `200` retorna `{ "unread_count": 0 }` em `data`.
