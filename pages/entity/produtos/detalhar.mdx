---
title: "Detalhar produto"
tag: "GET"
sidebarTitle: "Detalhar"
---

Consulte um produto pelo seu UUID.

<Badge color="blue">GET</Badge>

```text
/api/products/{uuid}
```

Use JWT. A resposta `200` retorna o produto em `data`. UUID inexistente ou associado a outro merchant retorna `404`.
